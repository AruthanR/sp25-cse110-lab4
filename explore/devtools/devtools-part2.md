1. The inputs from the text boxes are strings, not numbers. So, JavaScript concatenates the two numbers so 1 + 1 = 11.

2. To fix the code, I will need to convert the input values to numbers before performing addition using Number(). Screenshot of fix is fix.png in expand/screenshots.
